---
title: Observability
description: Deep dive into monitoring, metrics, logging, and alerting infrastructure.
---

### Observability

#### Challenge

Maintaining visibility into a system with 4 downstream services (CDN, Redis, PostgreSQL, Object Storage), 50+ application instances, and a 100:1 read/write ratio. The observability system must distinguish between expected behavior (normal cache misses, periodic cleanup) and anomalies (cache stampede, DB connection exhaustion, object storage degradation) without overwhelming storage with log volume. At 100M reads/day, logging every request produces ~50GB of log data per day — impractical to store in full.

**Observability vs. Monitoring**: Monitoring asks "is the system up?" (SLI/SLO tracking). Observability asks "why is the system behaving this way?" (tracing, structured logs, metrics correlation). Both are required: monitoring alerts the on-call engineer, observability tools help them diagnose the root cause.

#### Solution: Three Pillars — Metrics, Logs, Traces

**1. Metrics — Real-Time System Health**

| Category | Metric | Type | Labels | Target | Alert |
|----------|--------|------|--------|--------|-------|
| **Availability** | `http_requests_total\{status\}` | Counter | method, path, status, instance | — | P99 error rate > 1% |
| **Latency** | `http_request_duration_ms` | Histogram | method, path, instance | Read P95 < 100ms, Write P95 < 200ms | P95 > 150ms for 5 min |
| **Throughput** | `requests_per_second` | Gauge | direction (read/write), instance | ~1,157 rps read, ~12 rps write | — |
| **Cache** | `cache_hits_total` | Counter | tier (cdn/redis/lru), instance | Combined hit rate > 99% | Hit rate < 95% for 5 min |
| **Cache** | `cache_misses_total` | Counter | tier (cdn/redis/lru), instance | Combined hit rate > 99% | Hit rate < 95% for 5 min |
| **Database** | `db_queries_total` | Counter | operation (select/insert/update), shard | P95 < 5ms | P95 > 20ms for 2 min |
| **Database** | `db_connections_active` | Gauge | shard, role (primary/replica) | < 80% of pool max | > 90% pool usage |
| **Redis** | `redis_ops_total` | Counter | operation (get/set/del), node | < 2ms P95 | > 5ms for 2 min |
| **Object Storage** | `s3_requests_total` | Counter | operation (put/get/delete) | < 25ms P95 | > 50ms for 2 min |
| **Rate Limiting** | `rate_limit_hits_total` | Counter | identifier_type (ip/api_key) | < 5% of requests | > 10% — possible abuse |
| **Resource** | `cpu_usage` | Gauge | instance, az | < 70% sustained | CPU > 85% for 5 min |
| **Resource** | `memory_usage` | Gauge | instance, az | < 70% sustained | CPU > 85% for 5 min |
| **Resource** | `disk_usage` | Gauge | instance, az | < 70% sustained | CPU > 85% for 5 min |
| **Error** | `errors_total` | Counter | error_type, instance, stage | 0 for critical errors | Any critical error |

**2. Structured Logging — Request Correlation**

All logs are JSON-structured with a common request_id for correlation:

```json
{
  "timestamp": "2026-04-24T10:30:00.123Z",
  "level": "INFO",
  "service": "paste-service",
  "instance_id": "svc-01-us-east-1a",
  "request_id": "req-a1b2c3d4e5f6",
  "operation": "get_paste",
  "paste_key": "abc123",
  "latency_ms": 12.4,
  "cache_tier": "redis",
  "db_shard": 3,
  "access_mode": "public",
  "status": 200,
  "trace_id": "trace-9876543210"
}
```

**Log levels by operation**:
| Level | When | Volume |
|-------|------|--------|
| ERROR | Any unhandled exception, DB/Redis/S3 timeout, 5xx response | Low (< 0.1% of requests) |
| WARN | Slow queries (> 50ms), cache misses to DB, rate limit exceeded, unique constraint violation | Medium (~1% of requests) |
| INFO | Successful create/read/delete operations, admin actions, deployment events | High (all requests) |
| DEBUG | Detailed request/response bodies, Redis commands, SQL queries | Not enabled in production |

**Log retention**:
| Tier | Storage | Retention | Searchable |
|------|---------|-----------|------------|
| Hot | Elasticsearch / OpenSearch | 7 days | Yes (full-text search) |
| Warm | S3 + Athena | 90 days | Yes (SQL queries) |
| Cold | S3 Glacier | 1 year | On-demand restore |

**3. Distributed Tracing — Request Path Visualization**

OpenTelemetry instrumented across all Paste Service instances:

```
Client → CDN → API Gateway → Paste Service (instance-01)
                             ├── Redis GET paste:abc123 (2ms)
                             ├── PostgreSQL SELECT pastes WHERE paste_key (5ms)
                             ├── Object Storage GET objects/a7/abc123.txt (20ms)
                             └── Redis SET paste:abc123 metadata (2ms)
```

Trace spans capture:
- **Service span**: Paste Service request handling
- **DB span**: PostgreSQL query (with SQL statement redacted — never log raw content)
- **Cache span**: Redis get/set operations
- **Storage span**: Object storage get/put operations
- **External span**: CDN health check (if origin calls CDN APIs)

**Trace sampling**: Sample 100% of traces that contain errors (5xx, timeouts) and 10% of successful traces. This balances observability depth against storage cost.

**4. Dashboards**

| Dashboard | Audience | Key Panels |
|-----------|----------|------------|
| **Overview** | On-call engineer | Request rate, error rate, P95/P99 latency, cache hit rate, active connections |
| **Per-Component** | SRE/DBA | DB query latency per shard, Redis memory/ops, S3 latency, CDN cache hit rate |
| **Per-Shard** | DBA | Read/write throughput per shard, connection pool usage, replication lag |
| **Per-Instance** | SRE | CPU/memory/disk per instance, request rate per instance, error rate per instance |
| **User Experience** | Product team | P95/P99 latency by access mode (public/unlisted/private), CDN vs origin split |

**5. Alerts — PagerDuty Integration**

| Alert | Severity | Condition | Action |
|-------|----------|-----------|--------|
| `PasteServiceHighErrorRate` | P1 (Critical) | P99 error rate > 5% for 2 min | Auto-scale + notify on-call + Slack channel |
| `PasteServiceHighLatency` | P2 (High) | P95 latency > 150ms for 5 min | Notify on-call + Slack channel |
| `DatabaseConnectionPoolExhausted` | P1 (Critical) | Active connections > 90% pool max | Add replicas + notify DBA |
| `RedisNodeDown` | P2 (High) | Redis node unreachable for 30s | Re-provision node + notify SRE |
| `CacheHitRateDegraded` | P3 (Medium) | Combined hit rate < 90% for 10 min | Investigate cache warm logic + notify SRE |
| `ObjectStorageLatencyDegraded` | P3 (Medium) | S3 P95 > 50ms for 5 min | Notify SRE + check provider status |
| `CleanupJobStalled` | P3 (Medium) | Periodic cleanup job duration > 60s for 2 runs | Notify on-call |
| `RateLimitHitRateHigh` | P4 (Low) | Rate limit hits > 10% of requests for 5 min | Investigate abuse + notify security |

**6. SLI/SLO Tracking**

| SLI | SLO | Measurement Window | Current |
|-----|-----|-------------------|---------|
| Read availability (HTTP 200/204) | 99.95% | Rolling 30 days | — |
| Write availability (HTTP 201) | 99.9% | Rolling 30 days | — |
| Read latency P95 | < 100ms | Rolling 7 days | — |
| Write latency P95 | < 200ms | Rolling 7 days | — |
| Data durability | 99.99% | Rolling 90 days | — |

**Error budget**: 99.9% availability = 8.76 hours/year budget. At 100M requests/month, consuming 1% error budget = 1 million failures. Error budget tracking in Prometheus/Grafana with automated canary analysis.

**7. Debugging Playbook**

| Symptom | Investigation Tool | Likely Causes |
|---------|-------------------|---------------|
| P95 latency spike | Trace view → identify slowest span | DB connection pool exhaustion, Redis latency, S3 degradation |
| High error rate | Metrics dashboard → error type breakdown | 5xx responses, DB timeouts, Redis unavailability |
| Cache miss storm | Cache metrics + trace sampling | TTL expiry of popular paste, Redis node failure |
| Shard imbalance | Per-shard metrics | Uneven key distribution, hot paste on single shard |
| Rate limit 429 spike | Rate limit metrics + IP distribution | DDoS, scraping, misconfigured limits |

**8. Observability Infrastructure**

| Component | Choice | Role |
|-----------|--------|------|
| Metrics collection | Prometheus (scrapes) | Time-series metrics storage |
| Visualization | Grafana | Dashboards, alerting |
| Log aggregation | Elasticsearch/OpenSearch | Structured log search |
| Distributed tracing | Jaeger/Zipkin (OpenTelemetry) | Request path visualization |
| Alerting | PagerDuty (via Prometheus alertmanager) | On-call notification |
| Uptime monitoring | External (UptimeRobot/Grafana Cloud) | Multi-region health checks |

**9. Cost Optimization for Observability**

| Technique | Savings | Impact |
|-----------|---------|--------|
| Sample 10% of traces | ~90% storage reduction | Minimal observability loss (errors are 100% sampled) |
| Drop DEBUG logs in production | ~80% log volume reduction | No production impact |
| Log-only-successful-operations | ~60% log volume reduction (errors have more context) | Requires careful error logging |
| 7-day hot storage | Keeps recent data searchable without expensive hot storage | Older data archived to S3 |
| Per-second Prometheus scrape → 15s | ~15x metric reduction | Acceptable for non-critical metrics; keep 1s for latency/error rate |
